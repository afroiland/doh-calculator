export function convertForDisplay(crunchedNumbers) {
  console.log("crunchedNumbers: ", crunchedNumbers);

  let response = {
    soldiers: 0,
    productionCapacity: 0,
    productionRecommendation: 2,
    attackRecommendation: "everything"
  };

  return response;
}